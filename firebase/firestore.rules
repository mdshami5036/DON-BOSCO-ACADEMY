rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for Don Bosco Academy ERP & Portal client features
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
